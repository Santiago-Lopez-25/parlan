func fibonacci(n: int): int {
    if n < 2 {
        return n
    }
    return fibonacci(n - 1) + fibonacci (n - 2)
}

func main(): int {
    var fib20: int = fibonacci(20)
    c_code { printf("fib20: %d", usr_fib20); } //remember to add the 'usr_' sufix

    return 0
}